generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

// Tabela Usuario
model User {
  id   String @id @default(uuid()) // primary-key & guid
  nome String
}
